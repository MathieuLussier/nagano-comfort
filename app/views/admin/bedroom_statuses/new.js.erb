$('#page-content').empty();
$('#page-content').append('<%= escape_javascript(render partial: 'form') %>');
$('form#bedroom-status-form[data-remote="true"]').on('ajax:error', function(e, xhr, status, error) {
    alert(xhr.responseText, 'danger', 0);
});
